name: Build Nginx RPM for ARM64 (Kylin V10 Compatible)

on:
  workflow_dispatch:
    inputs:
      nginx_version:
        description: 'Nginx 版本号 (留空则自动获取最新稳定版)'
        required: false
        default: ''
        type: string

jobs:
  build-nginx-arm64-rpm:
    name: Build Nginx RPM for ARM64
    runs-on: ubuntu-24.04-arm
    
    steps:
      - name: 获取最新稳定版版本号
        id: get_version
        run: |
          if [ -n "${{ inputs.nginx_version }}" ]; then
            NGINX_VERSION="${{ inputs.nginx_version }}"
            echo "使用手动输入的版本: ${NGINX_VERSION}"
          else
            echo "正在获取 Nginx 最新稳定版本号..."
            NGINX_VERSION=$(curl -s https://nginx.org/en/download.html | grep -oP 'nginx-\K[0-9]+\.[0-9]+\.[0-9]+' | head -1)
            if [ -z "$NGINX_VERSION" ]; then
              echo "错误: 无法从官网获取版本号，请手动输入"
              exit 1
            fi
            echo "获取到最新稳定版本: ${NGINX_VERSION}"
          fi
          echo "nginx_version=${NGINX_VERSION}" >> $GITHUB_OUTPUT
          echo "build_date=$(date +'%Y%m%d_%H%M%S')" >> $GITHUB_OUTPUT
          echo "rpm_release=1" >> $GITHUB_OUTPUT
          
      - name: 安装 RPM 构建工具
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            rpm \
            rpm2cpio \
            cpio \
            wget \
            tar \
            curl \
            ca-certificates
          
      - name: 创建 RPM 构建目录结构
        run: |
          mkdir -p ~/rpmbuild/{BUILD,RPMS,SOURCES,SPECS,SRPMS}
          mkdir -p ~/rpmbuild/BUILDROOT
          echo "%_topdir $HOME/rpmbuild" > ~/.rpmmacros
          
      - name: 准备配置文件
        run: |
          # 创建临时目录存放配置文件
          mkdir -p ./configs/systemd
          mkdir -p ./configs/logrotate
          mkdir -p ./configs/nginx
          mkdir -p ./configs/html
          
          # 创建 systemd service 文件
          cat > ./configs/systemd/nginx.service << 'EOF'
          [Unit]
          Description=Nginx HTTP Server
          After=network.target remote-fs.target nss-lookup.target
          
          [Service]
          Type=forking
          PIDFile=/var/run/nginx.pid
          ExecStartPre=/usr/sbin/nginx -t
          ExecStart=/usr/sbin/nginx
          ExecReload=/usr/sbin/nginx -s reload
          ExecStop=/bin/kill -s QUIT $MAINPID
          PrivateTmp=true
          
          [Install]
          WantedBy=multi-user.target
          EOF
          
          # 创建 logrotate 配置
          cat > ./configs/logrotate/nginx << 'EOF'
          /var/log/nginx/*.log {
              daily
              missingok
              rotate 52
              compress
              delaycompress
              notifempty
              create 0640 nginx nginx
              sharedscripts
              postrotate
                  if [ -f /var/run/nginx.pid ]; then
                      kill -USR1 `cat /var/run/nginx.pid`
                  fi
              endscript
          }
          EOF
          
          # 创建 nginx.conf
          cat > ./configs/nginx/nginx.conf << 'EOF'
          user nginx;
          worker_processes auto;
          error_log /var/log/nginx/error.log warn;
          pid /var/run/nginx.pid;
          
          events {
              worker_connections 1024;
              use epoll;
              multi_accept on;
          }
          
          http {
              include /etc/nginx/mime.types;
              default_type application/octet-stream;
              
              log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                              '$status $body_bytes_sent "$http_referer" '
                              '"$http_user_agent" "$http_x_forwarded_for"';
              
              access_log /var/log/nginx/access.log main;
              
              sendfile on;
              tcp_nopush on;
              tcp_nodelay on;
              keepalive_timeout 65;
              types_hash_max_size 2048;
              client_max_body_size 10M;
              
              gzip on;
              gzip_vary on;
              gzip_proxied any;
              gzip_comp_level 6;
              gzip_types text/plain text/css text/xml text/javascript 
                         application/json application/javascript application/xml+rss 
                         application/rss+xml image/svg+xml;
              
              include /etc/nginx/conf.d/*.conf;
              
              server {
                  listen 80;
                  server_name localhost;
                  
                  location / {
                      root /usr/share/nginx/html;
                      index index.html index.htm;
                  }
                  
                  error_page 500 502 503 504 /50x.html;
                  location = /50x.html {
                      root /usr/share/nginx/html;
                  }
              }
          }
          EOF
          
          # 创建 mime.types
          cat > ./configs/nginx/mime.types << 'EOF'
          types {
              text/html                             html htm shtml;
              text/css                              css;
              text/xml                              xml;
              image/gif                             gif;
              image/jpeg                            jpeg jpg;
              application/javascript                js;
              application/atom+xml                  atom;
              application/rss+xml                   rss;
              text/plain                            txt;
              image/png                             png;
              image/svg+xml                         svg svgz;
              image/webp                            webp;
              image/x-icon                          ico;
              font/woff                             woff;
              font/woff2                            woff2;
              application/json                      json;
              application/pdf                       pdf;
              application/zip                       zip;
          }
          EOF
          
          # 创建默认站点配置
          cat > ./configs/nginx/default.conf << 'EOF'
          server {
              listen 80;
              server_name _;
              root /usr/share/nginx/html;
              index index.html;
          }
          EOF
          
          # 创建默认首页
          cat > ./configs/html/index.html << 'EOF'
          <!DOCTYPE html>
          <html>
          <head>
              <title>Nginx on ARM64</title>
              <style>
                  body {
                      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
                      text-align: center;
                      padding: 50px;
                      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                      color: white;
                      min-height: 100vh;
                      margin: 0;
                      display: flex;
                      align-items: center;
                      justify-content: center;
                  }
                  .container {
                      background: rgba(255,255,255,0.95);
                      color: #333;
                      padding: 40px;
                      border-radius: 20px;
                      box-shadow: 0 20px 40px rgba(0,0,0,0.1);
                      max-width: 600px;
                  }
                  h1 { color: #667eea; }
                  .version { 
                      background: #f0f0f0;
                      padding: 10px;
                      border-radius: 8px;
                      font-family: monospace;
                      margin: 20px 0;
                  }
                  .success { color: #4caf50; font-weight: bold; }
              </style>
          </head>
          <body>
              <div class="container">
                  <h1>✓ Nginx is Running!</h1>
                  <p>Successfully deployed on ARM64 architecture</p>
                  <div class="version">
                      Version: NGINX_VERSION_PLACEHOLDER<br>
                      Package: RPM for Kylin V10
                  </div>
                  <p class="success">✨ Built with GitHub Actions</p>
              </div>
          </body>
          </html>
          EOF
          
      - name: 使用 Docker 编译 Nginx
        run: |
          cat > Dockerfile << 'EOF'
          FROM ubuntu:20.04
          
          ENV DEBIAN_FRONTEND=noninteractive
          
          RUN apt-get update && apt-get install -y \
              build-essential \
              gcc \
              g++ \
              make \
              libpcre3 \
              libpcre3-dev \
              libssl1.1 \
              libssl-dev \
              zlib1g \
              zlib1g-dev \
              wget \
              tar \
              curl \
              ca-certificates \
              && rm -rf /var/lib/apt/lists/*
          
          WORKDIR /tmp
          
          ARG NGINX_VERSION
          ENV NGINX_VERSION=${NGINX_VERSION}
          
          # 下载并编译 Nginx
          RUN wget https://nginx.org/download/nginx-${NGINX_VERSION}.tar.gz && \
              tar -xzf nginx-${NGINX_VERSION}.tar.gz && \
              cd nginx-${NGINX_VERSION} && \
              ./configure \
                --prefix=/usr \
                --sbin-path=/usr/sbin/nginx \
                --conf-path=/etc/nginx/nginx.conf \
                --error-log-path=/var/log/nginx/error.log \
                --http-log-path=/var/log/nginx/access.log \
                --pid-path=/var/run/nginx.pid \
                --lock-path=/var/lock/nginx.lock \
                --user=nginx \
                --group=nginx \
                --with-http_ssl_module \
                --with-http_v2_module \
                --with-http_gzip_static_module \
                --with-http_stub_status_module \
                --with-threads \
                --with-cc-opt='-O2 -pipe -Wall -fstack-protector-strong' \
                --with-ld-opt='-Wl,-z,relro,-z,now' && \
              make -j$(nproc) && \
              make install DESTDIR=/tmp/nginx-install
          
          # 创建必要的目录
          RUN mkdir -p /tmp/nginx-install/usr/share/nginx/html && \
              mkdir -p /tmp/nginx-install/var/log/nginx && \
              mkdir -p /tmp/nginx-install/var/run/nginx && \
              mkdir -p /tmp/nginx-install/etc/nginx/conf.d && \
              mkdir -p /tmp/nginx-install/etc/systemd/system && \
              mkdir -p /tmp/nginx-install/etc/logrotate.d
          EOF
          
          # 构建 Docker 镜像
          docker build \
            --build-arg NGINX_VERSION=${{ steps.get_version.outputs.nginx_version }} \
            -t nginx-builder .
          
          # 从容器中复制编译产物
          docker create --name nginx-builder-container nginx-builder
          docker cp nginx-builder-container:/tmp/nginx-install ./nginx-install
          docker rm nginx-builder-container
          
      - name: 复制配置文件到编译产物
        run: |
          # 复制配置文件
          cp ./configs/systemd/nginx.service ./nginx-install/etc/systemd/system/
          cp ./configs/logrotate/nginx ./nginx-install/etc/logrotate.d/
          cp ./configs/nginx/nginx.conf ./nginx-install/etc/nginx/
          cp ./configs/nginx/mime.types ./nginx-install/etc/nginx/
          cp ./configs/nginx/default.conf ./nginx-install/etc/nginx/conf.d/
          cp ./configs/html/index.html ./nginx-install/usr/share/nginx/html/
          
          # 替换版本号占位符
          sed -i "s/NGINX_VERSION_PLACEHOLDER/${{ steps.get_version.outputs.nginx_version }}/g" ./nginx-install/usr/share/nginx/html/index.html
          
          # 创建 nginx 用户和组文件
          echo "nginx:x:997:995:nginx user:/usr/share/nginx:/sbin/nologin" > ./nginx-install/etc/passwd.nginx
          echo "nginx:x:995:" > ./nginx-install/etc/group.nginx
          
      - name: 验证编译产物
        run: |
          echo "=== 检查 nginx 二进制文件 ==="
          if [ -f ./nginx-install/usr/sbin/nginx ]; then
            file ./nginx-install/usr/sbin/nginx
            ./nginx-install/usr/sbin/nginx -v 2>&1 || true
            echo "✅ Nginx 编译成功"
          else
            echo "❌ Nginx 二进制文件不存在"
            exit 1
          fi
          
          echo ""
          echo "=== 检查配置文件 ==="
          ls -la ./nginx-install/etc/nginx/
          ls -la ./nginx-install/etc/systemd/system/
          ls -la ./nginx-install/etc/logrotate.d/
          
      - name: 准备 RPM 打包文件
        run: |
          NGINX_VER=${{ steps.get_version.outputs.nginx_version }}
          RPM_RELEASE=${{ steps.get_version.outputs.rpm_release }}
          
          BUILDROOT_DIR=~/rpmbuild/BUILDROOT/nginx-${NGINX_VER}-${RPM_RELEASE}.kylin.aarch64
          mkdir -p $BUILDROOT_DIR
          
          # 复制所有文件到 BUILDROOT
          cp -r ./nginx-install/* $BUILDROOT_DIR/
          
          # 确保必要的目录存在
          mkdir -p $BUILDROOT_DIR/var/log/nginx
          mkdir -p $BUILDROOT_DIR/var/run/nginx
          
          echo "=== BUILDROOT 验证 ==="
          find $BUILDROOT_DIR -type f -name "nginx" 2>/dev/null | head -5
          
      - name: 创建 RPM SPEC 文件
        run: |
          NGINX_VER=${{ steps.get_version.outputs.nginx_version }}
          RPM_RELEASE=${{ steps.get_version.outputs.rpm_release }}
          
          cat > ~/rpmbuild/SPECS/nginx.spec << 'SPEC_EOF'
          Name:           nginx
          Version:        VERSION_PLACEHOLDER
          Release:        RELEASE_PLACEHOLDER.kylin
          Summary:        High performance web server
          
          License:        BSD
          URL:            https://nginx.org
          
          BuildArch:      aarch64
          
          Requires:       pcre >= 8.0
          Requires:       openssl >= 1.0.2
          Requires:       zlib >= 1.2
          
          %description
          Nginx is a high performance web server and reverse proxy server.
          This package is built for ARM64 architecture with security patches.
          Compatible with Kylin V10.
          
          %files
          %defattr(-,root,root)
          /usr/sbin/nginx
          /etc/nginx
          /etc/systemd/system/nginx.service
          /etc/logrotate.d/nginx
          /usr/share/nginx
          %dir /var/log/nginx
          %dir /var/run/nginx
          
          %post
          # 创建 nginx 用户
          if ! getent group nginx > /dev/null 2>&1; then
              groupadd -r nginx
          fi
          if ! getent passwd nginx > /dev/null 2>&1; then
              useradd -r -g nginx -s /sbin/nologin -d /usr/share/nginx nginx
          fi
          
          # 设置权限
          chown -R nginx:nginx /var/log/nginx
          chown -R nginx:nginx /var/run/nginx
          
          # 启用服务
          systemctl daemon-reload
          
          %preun
          if [ $1 -eq 0 ]; then
              systemctl stop nginx
              systemctl disable nginx
          fi
          
          %postun
          if [ $1 -ge 1 ]; then
              systemctl try-restart nginx
          fi
          
          %changelog
          * Thu Jan 01 2026 GitHub Actions <actions@github.com> - VERSION_PLACEHOLDER-RELEASE_PLACEHOLDER
          - Built for ARM64 architecture
          - Includes security fixes for CVE-2026-42945
          - Compatible with Kylin V10
          SPEC_EOF
          
          # 替换版本号占位符
          sed -i "s/VERSION_PLACEHOLDER/${NGINX_VER}/g" ~/rpmbuild/SPECS/nginx.spec
          sed -i "s/RELEASE_PLACEHOLDER/${RPM_RELEASE}/g" ~/rpmbuild/SPECS/nginx.spec
          
      - name: 构建 RPM 包
        run: |
          cd ~/rpmbuild
          rpmbuild -bb SPECS/nginx.spec
          
      - name: 验证并准备 RPM 包
        run: |
          echo "=== 生成的 RPM 包 ==="
          find ~/rpmbuild -name "*.rpm" -type f
          
          # 查找并复制 RPM 文件
          RPM_FILE=$(find ~/rpmbuild -name "*.rpm" -type f | head -1)
          if [ -n "$RPM_FILE" ] && [ -f "$RPM_FILE" ]; then
            echo ""
            echo "=== RPM 包信息 ==="
            rpm -qip "$RPM_FILE" || true
            echo ""
            cp "$RPM_FILE" ./nginx-${{ steps.get_version.outputs.nginx_version }}.kylin.aarch64.rpm
            echo "✅ RPM 包创建成功: nginx-${{ steps.get_version.outputs.nginx_version }}.kylin.aarch64.rpm"
          else
            echo "⚠️ RPM 包创建失败"
            exit 1
          fi
          
      - name: 上传 RPM 包
        uses: actions/upload-artifact@v4
        with:
          name: nginx-kylin-rpm-${{ steps.get_version.outputs.nginx_version }}
          path: ./nginx-*.kylin.aarch64.rpm
          retention-days: 30
          
      - name: 生成安装说明
        run: |
          cat > INSTALL.md << 'EOF'
          # Nginx for Kylin V10 ARM64 安装说明
          
          ## 安装
          
          ```bash
          # 直接安装
          sudo rpm -ivh nginx-*.kylin.aarch64.rpm
          
          # 如果提示依赖问题，使用 --nodeps
          sudo rpm -ivh --nodeps nginx-*.kylin.aarch64.rpm
          
          # 启动服务
          sudo systemctl start nginx
          sudo systemctl enable nginx
          
          # 验证
          curl http://localhost
